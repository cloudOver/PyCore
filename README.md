PyCore - The CoureCluster API for Python
========================================
This repository was moved to CoreCluster gitlab. Please
contact us for details.

Installation
------------
Use pip3 command to install pycore library:
```pip3 install -U pycore```
